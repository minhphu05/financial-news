# =============================================================================
# API container — minimal dependency set (FastAPI + RAG stack only)
# =============================================================================

# --- Configuration -----------------------------------------------------------
python-dotenv==1.2.1
pydantic>=2.7
pydantic-settings>=2.4

# --- LLMs --------------------------------------------------------------------
openai>=1.30
voyageai>=0.3

# --- MongoDB -----------------------------------------------------------------
pymongo==4.15

# --- Qdrant ------------------------------------------------------------------
qdrant-client>=1.9

# --- API ---------------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
prometheus-client>=0.20

# --- Caching -----------------------------------------------------------------
redis[hiredis]>=5.0
httpx>=0.27
